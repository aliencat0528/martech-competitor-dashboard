> 繼承根目錄共用規則（Claude Code 已自動載入，勿重複讀取 ../CLAUDE.md）

# martech-competitor-dashboard — 專案規則

只記與根目錄規則的**差異**。

---

## 技術棧

| 層 | 選型 | 備註 |
|---|------|------|
| 建置 | Vite + React + TypeScript | v0.1.0 尚未建立，M1 落地 |
| 資料 | 靜態 JSON（`data/*.json`） | 無後端、無資料庫 |
| 圖表 | 手寫 SVG | 只需散佈圖／長條／熱力矩陣，不引入 D3 全家桶 |
| 匯出 | `@media print`（PDF）＋前端字串組裝（CSV／Markdown） | 零依賴 |
| 部署 | GitHub Pages 或 Vercel（靜態） | M1 決定 |

---

## Commit scopes

`data`、`docs`、`report`、`matrix`、`export`、`chart`、`schema`

範例：`feat(export): 加入 CSV 下載`、`docs(data): AIRIS 補上 G2 評論來源`

---

## 資料層硬規則（本專案的核心約束）

1. **沒有來源就不准進表**：`data/*.json` 每一筆事實欄位都必須帶
   `source_url`、`confidence`、`captured_at` 三個中繼欄位。缺任一個即視為未完成。
2. **`confidence` 只有四個值**：`verified`（第三方可佐證）、`claimed`（僅廠商單方說法）、
   `inferred`（公開資訊推論）、`none`（查不到）。**不得新增第五種**——多一種就會開始有人用它避開判斷。
3. **查不到就留空，不編**：`none` 的欄位值寫 `null`，不寫「約」「預估」「業界通常」。
   這條與根 `CLAUDE.md`「下結論前先查證」同源。
4. **廠商宣稱與第三方證據分開存**：同一件事有 `claimed_*` 與 `evidence_*` 兩組欄位，
   **不得合併成一欄取信度較高者**——兩者的落差正是本專案要量化的東西。
5. **不寫爬蟲**：資料一律手動維護。競品官網改版就壞的抓取腳本，維護成本會吃掉整個專案。
6. **不接 LLM 自動生成分析結論**：分析主張是本作品的核心價值，外包給模型等於把最值錢的部分讓掉。
   LLM 可用於整理素材，產出必須人工覆核並標註。

---

## 廠商資料的中立性

本專案的分析對象包含求職目標公司（Appier，← 根 `prepare.md` D-001）。

- 弱點與痛點**照寫**，不因為是目標公司就修飾——修飾過的競品分析在面試裡一問就破
- 但每一條批評都必須指得回公開來源，不得出現「我覺得」等級的斷言
- 廠商官網與新聞稿一律標 `claimed`，**不得因為寫得很肯定就升格為 `verified`**
