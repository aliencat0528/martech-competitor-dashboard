# martech-competitor-dashboard — 決策記錄

> **版本**：MC-011 · 2026-08-14
> 記錄規則繼承根 `prepare.md`，此處只寫差異：編號用 `MC-` 前綴，全域遞增、永不重用。
> 主線決策不複製理由，只記落地細節 + `← D-0XX` 指標。

---

## 決策日誌（新的在上）

### MC-011 · 2026-08-14 · scope
- **決策**：91APP 以**廠商層收錄**入 `data/vendors.json`——填財報、定位、產品線名稱，
  但**產品不進 `products.json`、能力不進矩陣**，能力字典維持 22 項不擴充
- **理由**：91APP 的核心能力（開店、金流、物流、庫存、POS、門市人員）一項都不在現有 22 項內，
  而 Appier 的 acquisition 群 5 項 91APP 全無。強行入矩陣就是 MC-005 明文棄選過的
  「不同市場的能力欄位放同一張矩陣，格子會大半是 N/A」。廠商層則沒有這個問題，財報與定位可直接比
- **代價**：91APP 在 Zone C 能力矩陣看不到，只出現在 Zone B 廠商快照列；
  MC-001「M1 只做一家」實質上已被打破，但打破的是廠商數不是 schema
- **棄選**：開第五群「零售電商平台側」並擴充能力字典至 28+ 項（M2 的工作量往前壓到 M1，
  且 Appier 在新欄位全空）；用分群專屬能力子集（最合 MC-005 精神，但 Zone C 要支援換欄位，M1 扛不動）
- **落地**：`vendors.json` 加 `record_scope` 欄位標明收錄層級與理由；
  可比範圍是 **Appier 個人化雲＋資料雲 vs 91APP Marketing Cloud＋AgentOne**，約交集 9 項能力，
  留待能力字典擴充後再進矩陣

### MC-010 · 2026-08-14 · ux
- **決策**：M1 的比較單位**下移到產品層級**（Appier 九產品互比能力覆蓋率、渠道數、垂直產業數、
  計價透明度）；Zone D 定位圖主軸改 **G2 Grid 式「市場影響力 × 產品覆蓋度」**四象限，
  兩軸權重**公開且讓讀者用滑桿自調**
- **理由**：M1 只有一家廠商，任何「廠商 vs 廠商」的圖都只有一個點。比較單位下移後 M1 就有真圖表，
  而「同一家公司九個產品彼此差多少」本身就是有內容的發現。權重可調是 Forrester Wave 的做法，
  它同時解掉「權重主觀所以不敢定」——改成公開可調就不必定死，而 L2 是純函式，權重只是參數
- **代價**：兩套比較單位（產品層／廠商層）並存，圖說模板要各寫一套；
  G2 Grid 式主軸要到 M2 對照組進來才真正成立
- **棄選**：圖表全延到 M2（M1 交不出 dashboard 的樣子）；畫單點佔位（符合現行規格但畫面很空）；
  雷達圖（面積隨軸序改變會誤導，且不在鎖定的三種圖型內）
- **落地**：Zone D 在 M1 先以產品層級軸上線，M2 換成廠商層級 G2 Grid 式；圖說「怎麼讀」隨軸換

### MC-009 · 2026-08-14 · scope
- **決策**：**撤銷 MC-007**。不再以「宣稱 vs 證據落差分」為核心指標，改用**客觀可查的指標組**：
  能力覆蓋率、產品線重疊率、渠道數、垂直產業數、整合生態系數、合規認證、計價透明度、
  財報規模、評論數與評分
- **理由**：用戶明確指出本專案要比的是功能差異與市場使用，不是指出對手誇大。
  落差分的「宣稱強度」無法客觀量化，MC-007 自己的代價欄就預告了「評分主觀性高……否則整個指標站不住」
- **代價**：失去 MC-007 的差異化，退回 MC-007 棄選欄警告過的
  「只做功能覆蓋率比較（客觀但沒有觀點，等同競品官網的比較表）」。
  **對策是把觀點移到 Zone A 人寫的關鍵發現**（仍強制帶 `evidence_ids`），指標本身保持客觀
- **棄選**：保留落差分但改成二元計數「無證據支撐的宣稱數」（仍在談誇大，非用戶要的方向）；
  維持 MC-007 原案（評分站不住，過不了 M3 出口條件「這 0.5 分怎麼來的」）
- **落地**：`claimed_strengths` / `evidence_reviews` 兩組欄位**保留不刪**，
  降級為 Zone E 的口碑素材，不再相減成分數；Zone C 的 `claimed_only` 語意由「指控」改為單純信度標記。
  **9 份產品資料一筆都不用重填**

### MC-008 · 2026-08-11 · ux
- **決策**：每張圖下方固定兩塊圖說——**「怎麼讀」**（靜態，人寫，隨圖不隨資料）與
  **「這張圖現在說什麼」**（隨資料變）。後者再分 `summary`（**L2 分析層句型模板＋數字填空**，必填）
  與 `takeaway`（人寫主張，選填，必帶 `evidence_ids`）。**沒有圖說的圖不准上線**
- **理由**：圖會被截圖、被匯出、被貼進簡報，離開頁面後就只剩它自己——沒有圖說的圖，
  在面試現場只能靠口頭補充，補充不到的人就看不懂。
  `summary` 之所以走模板而非人寫，是因為本專案是週期性快照（← MC-003），
  人寫的描述會在下次更新資料時默默過期，且過期得無聲無息
- **代價**：句型模板要為每張圖各寫一套，且切換軸／套篩選都要重算；
  模板句讀起來比人寫的生硬
- **棄選**：只寫靜態「怎麼讀」（圖例說明而已，讀者仍要自己從圖裡看出結論）；
  `summary` 也人寫（會過期，而且是最難察覺的那種錯——數字變了文字沒變，看起來完全正常）；
  用 LLM 依當前資料生成描述（違反 `CLAUDE.md` 資料層硬規則 6；
  **模板填空不算生成**：句型是人寫死的，模型不參與，數字來自 L2）
- **落地**：Zone C 的「怎麼讀」直接引用 `capabilities.json` 的 `cell_states[].definition` 原文，
  與 Zone F 方法論頁共用同一組定義；「怎麼讀」同時作為圖表的 `aria-describedby`，不另寫替代文字

### MC-007 · 2026-08-07 · scope
- **決策**：以「**宣稱強度 vs. 證據強度**」的落差分作為本專案的差異化指標，
  資料層用 `claimed_*` 與 `evidence_*` 兩組欄位分開存，**不合併取信度較高者**
- **理由**：功能表格化誰都做得出來，能撐起分析主張的是「指出對手自己說不圓的地方」。
  Appier 恰好有現成證據：官方主打 AIRIS+AIQUA 閉環，G2 評論卻指出自家產品間資料交換不順
- **代價**：評分主觀性高，方法論頁（M3）必須寫清楚每一分怎麼給，否則整個指標站不住
- **棄選**：只做功能覆蓋率比較（客觀但沒有觀點，等同競品官網的比較表）；
  合併成單一「可信度」欄（落差被抹平，就再也看不到廠商說了什麼卻拿不出證據）

### MC-006 · 2026-08-07 · scope
- **決策**：PDF、CSV、Markdown **三種匯出在 M1 一次做齊**，不分期
- **理由**：用戶明示匯出是核心需求（「可以匯出報告」）；三者實作路徑各自獨立
  （print stylesheet／字串組裝／字串組裝），零共用依賴，分期只會多一次回頭成本
- **代價**：M1 多約一個工作單元
- **棄選**：PDF 優先其餘 M3 補（匯出是需求本體不是加分項，延後等於 M1 交不出完整東西）

### MC-005 · 2026-08-07 · scope
- **決策**：M2 對照組**分四群收錄**，不混成一張平表——
  ①國際 engagement 大廠（Braze／Insider／Bloomreach）②台灣在地 LINE 生態（漸強實驗室／Super 8／omnichat）
  ③廣告投放側（Moloco／Criteo／AppLovin）④CDP 側（Segment／Treasure Data／Tealium）
- **理由**：Appier 橫跨廣告、互動、資料三個市場，任一單群對照都只能比到它三分之一的產品線。
  分群後每群各自回答一個問題，四群合起來才是「Appier 到底在跟誰打」
- **代價**：M2 資料量是單群的四倍；各群公開資料完整度差很多（台灣在地那群最少）
- **棄選**：只收國際大廠（比不到 BotBonnie 這條護城河）；只收台灣在地（沒有份量、
  講不出產業級結論）；混成一張平表（不同市場的能力欄位放同一張矩陣，格子會大半是 N/A）

### MC-004 · 2026-08-07 · schema
- **決策**：`data/*.json` 每筆事實欄位強制帶 `source_url` / `confidence` / `captured_at` 三個中繼欄位；
  `confidence` 固定四態 `verified` / `claimed` / `inferred` / `none`，**不得新增第五種**
- **理由**：競品情報最常見的失敗是「表格填滿了但沒人記得哪格是猜的」。把來源設成 schema 必填，
  查不到就只能留 `null`——這比任何自律規則都有效。四態封閉是因為多一種就會有人用它避開判斷
- **代價**：填資料變慢，且會有明顯留白的欄位（使用率、定價幾乎全是 `none`）
- **棄選**：單一布林「已驗證」（分不出「廠商說的」與「查不到」，而這兩者的處理方式完全不同）；
  五態含「部分驗證」（實測會變成所有不確定的東西的垃圾桶）

### MC-003 · 2026-08-07 · tech
- **決策**：Vite + React + TypeScript，資料走**靜態 JSON**，無後端、無資料庫、無爬蟲；
  圖表手寫 SVG，部署 GitHub Pages 或 Vercel
- **理由**：競品情報本質是**週期性人工快照**，不是即時資料流——標好抓取日期比接資料庫更專業。
  後端只會拖慢作品完成度，而作品集專案沒有外力推著完成，停在半成品不會有訊號提醒（← 同 D-016 的判斷）
- **代價**：資料更新要改 JSON 後重新部署；筆數上千時前端載入要另想辦法（M2 之後才會遇到）
- **棄選**：接爬蟲自動更新（競品官網改版就壞，維護成本吃掉整個專案）；上後端＋DB
  （與分析價值無關的工程量）；用 D3 全家桶（只需三種圖，套件比自寫 SVG 還大）

### MC-002 · 2026-08-07 · ux
- **決策**：**單頁、報告在最上方**的形態——Zone A 報告區（執行摘要／關鍵發現／版本 diff／匯出鈕），
  下接 B 廠商快照列、C 能力矩陣、D 定位圖、E 產品明細抽屜、F 來源與方法論。
  報告區每句結論掛錨點連到下方證據列
- **理由**：用戶明示「競品分析報告在最上方」。更重要的是這個順序決定了作品的性質：
  先給結論再給資料＝分析作品；先給圖表再讓人自己看＝BI 工具，後者作品集裡到處都是
- **代價**：單頁會長，需要 sticky 導覽；報告區與資料區的一致性要靠分析層保證，不能手寫
- **棄選**：多頁式（結論與證據被分頁切開，「從所以呢點到憑什麼」這條路就斷了）；
  圖表牆（沒有主張，做完講不出一句話）

### MC-001 · 2026-08-07 · scope
- **決策**：M1 **只做 Appier 一家做深**（九產品＋八 Agent 全入庫、報告區可讀、三種匯出可用），
  對照組整批延到 M2
- **理由**：資料層 schema 必須先被一家真實廠商的完整資料壓過一遍才知道哪裡不夠用；
  用四家一起試錯，schema 改一次就要回頭改四份資料
- **代價**：M1 完成時嚴格說還不能叫「競品」dashboard，只能叫單廠商情報頁——
  **這一點在 M1 的 README 與報告區都要講明，不能靠命名混過去**
- **棄選**：一開始鋪四家做橫向比較（一步到位符合命名，但資料量四倍、M1 拖長，
  且各家公開資料完整度不一，矩陣會大半是空格，看起來像沒做完而不是像沒資料）

### MC-000 · 2026-08-07 · structure
- **決策**：立項本專案，依 polyrepo 走獨立 GitHub repo、**public**（← D-002、← D-021）
- **落地**：v0.1.0 只寫文件與資料層，**不寫實作程式碼**——比照 `goal-drift-alert` 立項時的做法
- **public 的邊界**：本專案資料全部來自公開來源（廠商官網、新聞稿、財報、G2 評論），
  無個資、無內部資訊，故不需要 `data/` 的 gitignore 例外（與 `goal-drift-alert` 不同）
- **與既有 martech 專案的關係**：`martech-trend-agent` 看產業風向、`martech-job-radar` 看職缺，
  本專案看**產品**。三者資料不共用——前兩者的資料源是職缺與新聞，本專案是廠商產品頁與評論站

---

## 待討論事項

| # | 議題 | 卡在什麼 | 提出日期 |
|---|------|---------|---------|
| 2 | 資料更新頻率與版本 diff 的基準 | Zone A 要顯示「與上一版的差異」，但尚未決定快照是每月一版還是事件驅動（廠商發新品才更新）。**真正卡架構的是「要不要 `data/` 下多一層日期目錄」，這與頻率可分開決定**；不決定，M3 做 diff 時要回頭搬檔案且所有路徑要改 | 2026-08-07 |
| 3 | 台灣在地那群（漸強／Super 8／omnichat）公開資料不足時怎麼辦 | 三家都沒有 G2 規模的評論樣本，若照 MC-004 誠實留 `none`，該群矩陣會幾乎全空——是接受留白，還是改用不同的欄位組。**91APP 已實際撞到這題**（`g2_rating` 為 `none`） | 2026-08-07 |
| 4 | 對照組的納入門檻沒有明文標準 | MC-005 列了四群的名單，但沒寫「憑什麼收這幾家」。業界慣例（Gartner／Forrester）都會明訂 inclusion criteria（營收或客戶數門檻），沒有標準就會被問「為什麼收 A 不收 B」 | 2026-08-14 |
| 5 | 能力字典要不要擴充零售側欄位 | MC-011 暫不擴充，91APP 因此進不了 Zone C。M2 收對照組時要決定：擴充成大字典、或改採分群專屬子集 | 2026-08-14 |

> 議題 1（落差分評分規則）已隨 MC-009 撤銷 MC-007 而消滅，不再列管。
